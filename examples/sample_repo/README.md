# Sample Repo

A small fixture repository used by DevPilot AI demos, backend tests, and Playwright e2e flows.

## Structure

- `src/` — Python package with a couple of modules
- `tests/` — a minimal pytest suite
- `config/` — sample configuration

## Stack

- Python
- FastAPI-flavored app module
